// import { useState } from "react";
const Score = ({ score, maxScore }) => {
  return (
    <>
      <h2>score = {score}</h2>
      <h2>max score = {maxScore}</h2>
    </>
  );
};

export default Score;
